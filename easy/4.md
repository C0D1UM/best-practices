The code under the ```else``` block after a ```try``` executes **only** when the code under the ```try``` block does **not** raise an exception, where as code under a ```finally``` block **always** executes.
